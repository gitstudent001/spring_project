// editPost.js
document.addEventListener("DOMContentLoaded", function() {
  // 공통 초기화가 이미 수행되었으므로 여기에서는 옵션 인덱스만 업데이트합니다.
  if (document.getElementById('ranking-options')) {
    updateOptionIndexes();
  }
});

